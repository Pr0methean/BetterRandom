parameters:
  packageManager: 'sudo apt'
steps:
- script: '${{ parameters.packageManager }} install tor'
  displayName: Install Tor
- script: sudo service tor start
  displayName: Start Tor
- script: ./unit-tests.sh
  displayName: Build & Test
  env:
    RANDOM_DOT_ORG_KEY: $(RANDOM_DOT_ORG_KEY)
