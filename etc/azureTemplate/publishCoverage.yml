steps:
  - task: PublishCodeCoverageResults@1
    inputs:
      codeCoverageTool: 'JaCoCo' # Options: cobertura, jaCoCo
      summaryFileLocation: betterrandom/target/jacoco.xml
      reportDirectory: betterrandom/target
      failIfCoverageEmpty: true